name: CI

on:
  push:
    tags-ignore: ["v*"]
  pull_request:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Toolchain versions
        run: |
          xcodebuild -version
          swift --version

      - name: Build app
        run: |
          cd PortKilla
          chmod +x scripts/build.sh
          ./scripts/build.sh
          test -d dist/PortKilla.app
